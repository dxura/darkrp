@namespace Dxura.Darkrp.UI
@inherits Panel

<root>
	<h1>TODO Market</h1>
	
	@if ( PlayerState.Local?.Job.Name == "Gun Dealer" )
	{
		<BuyMenu></BuyMenu>
	}
</root>

@code
{

	protected override int BuildHash()
	{
		return HashCode.Combine( PlayerState.Local?.Job.Name );

	}
}
